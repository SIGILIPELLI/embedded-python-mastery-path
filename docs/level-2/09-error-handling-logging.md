# Error Handling & On-Device Logging

!!! info "Coming soon"
    This module's full lessons and code snippets are being written next.
    This module will cover exception handling patterns for unattended devices, `sys.print_exception`, rotating log files on flash, capturing tracebacks across resets, and `machine.reset_cause()`.

More lessons are on the way — check back soon.
